# ─── OpenAgents Environment Variables ───

# API key for the headless POST /api/agent endpoint.
# Nuxt maps NUXT_ prefixed vars to runtimeConfig automatically.
# Leave empty to disable the REST agent API (WebSocket UI still works).
NUXT_OPENAGENTS_API_KEY=

# Transport backend: "local" (child_process) or "docker" (dockerode).
# Default: local
TRANSPORT=local
